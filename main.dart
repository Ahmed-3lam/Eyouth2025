import 'dart:ffi';

void main() {
  List<String> names = ["Ahmed", "Mohamed", "Alaa", "AA"];
  for (int i = 0; i < names.length; i++) {
  
    // print(names[i]);
  }

  for (var item in names) {
    print(item);
  }
}
